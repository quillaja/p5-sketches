// a basic p5.js example that creates a canvas,
// changes the bg color, and draws a circle (ellipse)

function setup() {
    createCanvas(600,600)
}

function draw() {
    background(51)
    ellipse(50,50,10,10)
}